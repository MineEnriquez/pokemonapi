
const Poke = require('../controllers/ctrl_poke');
module.exports = function (app) {
    // app.get('/', (req, res) => {General.index(req, res);});
    // app.post('/api/pokes/newpoke', (req, res) => {Poke.newpoke(req, res);});
    // app.get('/api/pokes/retrieveall', (req,res)=> {Poke.retrieveAll(req, res);});
    // app.get('/api/pokes/retrieveId/:id', (req,res)=> {Poke.retrieveId(req, res);});
    // app.put('/api/pokes/Update/:id', (req,res)=> {Poke.updateId(req, res);});
    // app.delete('/api/pokes/Delete/:id', (req,res)=> {Poke.deleteId(req, res);});
}